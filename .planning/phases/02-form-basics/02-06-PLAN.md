---
phase: 02-form-basics
plan: 06
type: execute
wave: 4
depends_on:
  - 02-05
files_modified:
  - packages/ui/src/registry/versions/0.5.0/components/field.tsx
  - packages/ui/src/registry/versions/0.5.0/components/input.tsx
  - packages/ui/src/registry/versions/0.5.0/components/text-area.tsx
  - packages/ui/src/registry/versions/0.5.0/registry.json
autonomous: true
requirements:
  - FORM-01
  - FORM-04
gap_closure: true

must_haves:
  truths:
    - "Error text passed as errorText prop to Field renders visibly in the DOM"
    - "Error text passed as errorText prop to Input renders visibly in the DOM"
    - "Error text passed as errorText prop to TextArea renders visibly in the DOM"
    - "registry.json input entry does not list lucide-react as a dep"
  artifacts:
    - path: "packages/ui/src/registry/versions/0.5.0/components/field.tsx"
      provides: "Field component — FORM-01 with working error display"
      contains: "match={true}"
    - path: "packages/ui/src/registry/versions/0.5.0/components/input.tsx"
      provides: "Input component — FORM-04 with working error display"
      contains: "match={true}"
    - path: "packages/ui/src/registry/versions/0.5.0/components/text-area.tsx"
      provides: "TextArea component with working error display"
      contains: "match={true}"
    - path: "packages/ui/src/registry/versions/0.5.0/registry.json"
      provides: "Registry manifest with correct deps for input"
      contains: "\"@base-ui/react\", \"clsx\", \"tailwind-merge\""
  key_links:
    - from: "field.tsx BaseField.Error"
      to: "Base UI FieldError render condition"
      via: "match={true} bypasses ValidityState check"
      pattern: "match=\\{true\\}"
    - from: "input.tsx Field.Error"
      to: "Base UI FieldError render condition"
      via: "match={true} bypasses ValidityState check"
      pattern: "match=\\{true\\}"
    - from: "text-area.tsx Field.Error"
      to: "Base UI FieldError render condition"
      via: "match={true} bypasses ValidityState check"
      pattern: "match=\\{true\\}"
---

<objective>
Fix the three blocker gaps identified in VERIFICATION.md: `BaseField.Error` / `Field.Error` components in field.tsx, input.tsx, and text-area.tsx never render their children because they are missing `match={true}`. Also remove the erroneous `lucide-react` dep from the `input` registry entry.

Purpose: FORM-01 (Field) and FORM-04 (Input) error text display is silently broken. After this plan, passing `errorText` to Field, Input, or TextArea will render a visible error message below the control.
Output: Three component files with `match={true}` added; registry.json with corrected input deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-form-basics/02-VERIFICATION.md
@.planning/phases/02-form-basics/02-REVIEW.md

<interfaces>
<!-- Root cause from VERIFICATION.md / REVIEW.md -->
<!--
Base UI's FieldError component (the underlying implementation of Field.Error and BaseField.Error)
renders based on:
  validityData.state.valid === false

Setting invalid={!!errorText} on Field.Root changes the field's visual invalid state but does NOT
change validityData.state.valid from null to false — that only happens via native HTML5 constraint
validation (required, pattern, etc.).

The fix: pass match={true} on every Field.Error / BaseField.Error call. This prop instructs Base UI
to always render the element when the parent Field.Root is in the invalid state, bypassing the
ValidityState check entirely.

Exact lines to modify:
  field.tsx   line 56: <BaseField.Error          → <BaseField.Error match={true}
  input.tsx   line 139: <Field.Error             → <Field.Error match={true}
  text-area.tsx line 131: <Field.Error           → <Field.Error match={true}
-->

From packages/ui/src/registry/versions/0.5.0/components/field.tsx (current, broken):
```tsx
{errorText && (
  <BaseField.Error
    className={twMerge("text-sm text-error", classNames?.errorText)}
  >
    {errorText}
  </BaseField.Error>
)}
```

Fixed (add match={true}):
```tsx
{errorText && (
  <BaseField.Error
    match={true}
    className={twMerge("text-sm text-error", classNames?.errorText)}
  >
    {errorText}
  </BaseField.Error>
)}
```

From packages/ui/src/registry/versions/0.5.0/components/input.tsx (current, broken):
```tsx
{errorText && (
  <Field.Error
    id={errorId}
    className={twMerge("text-sm text-error", classNames?.errorText)}
  >
    {errorText}
  </Field.Error>
)}
```

Fixed (add match={true}):
```tsx
{errorText && (
  <Field.Error
    id={errorId}
    match={true}
    className={twMerge("text-sm text-error", classNames?.errorText)}
  >
    {errorText}
  </Field.Error>
)}
```

From packages/ui/src/registry/versions/0.5.0/components/text-area.tsx (current, broken):
```tsx
{errorText && (
  <Field.Error
    id={errorId}
    className={twMerge("text-sm text-error", classNames?.errorText)}
  >
    {errorText}
  </Field.Error>
)}
```

Fixed (add match={true}):
```tsx
{errorText && (
  <Field.Error
    id={errorId}
    match={true}
    className={twMerge("text-sm text-error", classNames?.errorText)}
  >
    {errorText}
  </Field.Error>
)}
```

From packages/ui/src/registry/versions/0.5.0/registry.json — input entry (current, incorrect):
```json
"input": {
  "deps": ["@base-ui/react", "clsx", "lucide-react", "tailwind-merge"],
  ...
}
```

Fixed (input.tsx does not import lucide-react; icon prop is React.ReactNode):
```json
"input": {
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add match={true} to Field.Error in field.tsx, input.tsx, and text-area.tsx</name>
  <files>
    packages/ui/src/registry/versions/0.5.0/components/field.tsx
    packages/ui/src/registry/versions/0.5.0/components/input.tsx
    packages/ui/src/registry/versions/0.5.0/components/text-area.tsx
  </files>
  <read_first>
    - packages/ui/src/registry/versions/0.5.0/components/field.tsx (READ FIRST — see current BaseField.Error at lines 55-61; the fix is adding match={true} as a prop on line 56)
    - packages/ui/src/registry/versions/0.5.0/components/input.tsx (READ FIRST — see current Field.Error at lines 138-145; the fix is adding match={true} as a prop on line 139)
    - packages/ui/src/registry/versions/0.5.0/components/text-area.tsx (READ FIRST — see current Field.Error at lines 130-137; the fix is adding match={true} as a prop on line 131)
  </read_first>
  <action>
Make three surgical changes — one per file. Each change adds only `match={true}` to an existing JSX element. Do NOT change any other code.

**field.tsx — line 56:**
Change:
```tsx
<BaseField.Error
  className={twMerge("text-sm text-error", classNames?.errorText)}
>
```
To:
```tsx
<BaseField.Error
  match={true}
  className={twMerge("text-sm text-error", classNames?.errorText)}
>
```

**input.tsx — line 139:**
Change:
```tsx
<Field.Error
  id={errorId}
  className={twMerge("text-sm text-error", classNames?.errorText)}
>
```
To:
```tsx
<Field.Error
  id={errorId}
  match={true}
  className={twMerge("text-sm text-error", classNames?.errorText)}
>
```

**text-area.tsx — line 131:**
Change:
```tsx
<Field.Error
  id={errorId}
  className={twMerge("text-sm text-error", classNames?.errorText)}
>
```
To:
```tsx
<Field.Error
  id={errorId}
  match={true}
  className={twMerge("text-sm text-error", classNames?.errorText)}
>
```

No other changes to any of these files. Do not reformat, reorder, or modify anything beyond the three prop additions.
  </action>
  <verify>
    <automated>grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/field.tsx packages/ui/src/registry/versions/0.5.0/components/input.tsx packages/ui/src/registry/versions/0.5.0/components/text-area.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/field.tsx` returns `1`
    - `grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/input.tsx` returns `1`
    - `grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/text-area.tsx` returns `1`
    - field.tsx: `match={true}` appears on `BaseField.Error` (the element inside the `{errorText && ...}` block)
    - input.tsx: `match={true}` appears on `Field.Error` (the element inside the `{errorText && ...}` block)
    - text-area.tsx: `match={true}` appears on `Field.Error` (the element inside the `{errorText && ...}` block)
    - `pnpm --filter @bctechnology/ui test` still passes with no new failures
  </acceptance_criteria>
  <done>All three Field.Error / BaseField.Error calls have match={true}. Base UI will now render error children whenever Field.Root has invalid={true}, bypassing the ValidityState check. Error text will appear for users passing the errorText prop.</done>
</task>

<task type="auto">
  <name>Task 2: Remove lucide-react from input registry entry</name>
  <files>packages/ui/src/registry/versions/0.5.0/registry.json</files>
  <read_first>
    - packages/ui/src/registry/versions/0.5.0/registry.json (READ FIRST — the current input entry at line 66-73 has deps: ["@base-ui/react", "clsx", "lucide-react", "tailwind-merge"]; remove "lucide-react" from this array only)
    - packages/ui/src/registry/versions/0.5.0/components/input.tsx (READ FIRST — confirm the file has no `import ... from "lucide-react"` anywhere; the icon prop is React.ReactNode and does not import lucide-react)
  </read_first>
  <action>
In registry.json, find the `"input"` key. Its current `"deps"` array is:
```json
["@base-ui/react", "clsx", "lucide-react", "tailwind-merge"]
```

Change it to:
```json
["@base-ui/react", "clsx", "tailwind-merge"]
```

Remove `"lucide-react"` from this array only. Do NOT modify any other entry in registry.json. All other 11 entries must remain exactly as they are. The JSON must remain valid (no trailing commas).

Rationale: input.tsx accepts `icon?: React.ReactNode` — consumers instantiate their own icon JSX. The component itself does not import lucide-react. Installing it as a dep wastes the consumer's install.
  </action>
  <verify>
    <automated>node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.input.deps.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.input.deps.includes('lucide-react'))"` outputs `false`
    - `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.input.deps.join(','))"` outputs `@base-ui/react,clsx,tailwind-merge`
    - `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(Object.keys(r).length)"` outputs `12` (no entries added or removed)
    - `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.avatar.deps.includes('lucide-react'))"` outputs `true` (avatar entry unchanged)
    - registry.json is valid JSON (node -e parses without throwing)
    - `pnpm --filter @bctechnology/ui test` passes
  </acceptance_criteria>
  <done>The input registry entry no longer lists lucide-react as a dep. `bct add input` will not install lucide-react for consumers who don't need it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer props → Field.Error render | errorText prop value is rendered as text inside Field.Error — no HTML injection risk since React escapes string children |
| registry.json → bct CLI | CLI reads registry.json to resolve deps; removing lucide-react reduces unnecessary package installations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | field.tsx / input.tsx / text-area.tsx | accept | match={true} is a static boolean literal; no user data flows through it; the change enables rendering of consumer-supplied errorText strings which React already escapes |
| T-02-16 | Information Disclosure | errorText prop rendering | accept | Error text content is consumer-supplied and intentionally visible; rendering it is the desired behavior; no server secrets cross this boundary |
| T-02-17 | Tampering | registry.json deps | mitigate | Removal of lucide-react from input deps is validated in acceptance criteria; node -e parse check confirms valid JSON after edit |
</threat_model>

<verification>
After both tasks complete:
- `grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/field.tsx` → `1`
- `grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/input.tsx` → `1`
- `grep -c 'match={true}' packages/ui/src/registry/versions/0.5.0/components/text-area.tsx` → `1`
- `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.input.deps.includes('lucide-react'))"` → `false`
- `pnpm --filter @bctechnology/ui test` → all tests pass, no regressions
</verification>

<success_criteria>
All three blocker gaps are resolved: Field.Error, Field.Error in Input, and Field.Error in TextArea each have match={true} and will render error children when the parent Field.Root is marked invalid. The input registry entry no longer incorrectly lists lucide-react as a dep.
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-basics/02-06-SUMMARY.md`
</output>

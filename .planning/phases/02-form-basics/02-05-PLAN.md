---
phase: 02-form-basics
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - packages/ui/src/registry/versions/0.5.0/registry.json
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04
  - FORM-05
  - FORM-06
  - FORM-07

must_haves:
  truths:
    - "registry.json contains 12 entries total (5 Phase 1 + 7 Phase 2)"
    - "Each Phase 2 component has a valid registry entry with correct deps and registryDeps"
    - "Only input and text-area entries include lucide-react in deps — no other Phase 2 component uses icons"
    - "All 7 Phase 2 entries have registryDeps: []"
  artifacts:
    - path: "packages/ui/src/registry/versions/0.5.0/registry.json"
      provides: "Registry entries for all 7 Phase 2 components"
      contains: "\"field\""
  key_links:
    - from: "registry.json"
      to: "components/field.tsx"
      via: "files[0].src: components/field.tsx"
      pattern: "\"field\""
    - from: "registry.json"
      to: "components/input.tsx"
      via: "files[0].src: components/input.tsx, deps includes lucide-react"
      pattern: "\"input\""
---

<objective>
Add 7 Phase 2 registry entries to `registry.json`. This is the final consolidation step — all component files from Plans 02, 03, and 04 are now in place, so the shared registry.json can be updated in a single pass without file-ownership conflicts.

Purpose: Make all 7 Phase 2 components discoverable and installable via `bct add <component>`. Completing INFRA-02 for this phase.
Output: Updated `registry.json` with 12 total entries (5 Phase 1 + 7 Phase 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-form-basics/02-RESEARCH.md

<interfaces>
<!-- Exact registry.json format from existing Phase 1 entries. -->

From packages/ui/src/registry/versions/0.5.0/registry.json (current shape):
```json
{
  "button": {
    "title": "Button",
    "description": "...",
    "category": "form-inputs",
    "files": [{ "src": "components/button.tsx", "dst": "button.tsx" }],
    "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
    "registryDeps": []
  }
}
```

Phase 2 entries to add — deps specification:
- field, fieldset, form, switch, toggle: deps = ["@base-ui/react", "clsx", "tailwind-merge"]
- input: deps = ["@base-ui/react", "clsx", "lucide-react", "tailwind-merge"]
  (lucide-react included because Input accepts icon?: React.ReactNode — consumers will pass Lucide icons)
- text-area: deps = ["@base-ui/react", "clsx", "tailwind-merge"]
  (no lucide-react — TextArea has no icon prop per D-02)

NOTE on lucide-react for input: The `icon` prop is React.ReactNode so consumers pass pre-instantiated JSX. Including lucide-react in deps ensures the consumer's project has it available. Other Phase 2 components do not use icons.

All 7 Phase 2 entries: registryDeps = []
All 7 Phase 2 entries: category = "form-inputs"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 Phase 2 entries to registry.json</name>
  <files>packages/ui/src/registry/versions/0.5.0/registry.json</files>
  <read_first>
    - packages/ui/src/registry/versions/0.5.0/registry.json (READ FIRST — the current file has 5 entries; you will add 7 more to this JSON object; do NOT overwrite existing entries)
    - .planning/phases/02-form-basics/02-RESEARCH.md (registry.json entries section — exact format for all 7 Phase 2 entries, with deps per component)
  </read_first>
  <action>
Read the current registry.json, then add 7 new keys inside the top-level JSON object. The result is a flat JSON object with 12 keys total.

Add these 7 entries (add them after the existing 5 Phase 1 entries, maintaining JSON object structure):

```json
"field": {
  "title": "Field",
  "description": "Form field container with label, helper text, error text, and required marker. Wraps Base UI Field primitives for automatic aria association.",
  "category": "form-inputs",
  "files": [{ "src": "components/field.tsx", "dst": "field.tsx" }],
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  "registryDeps": []
},
"fieldset": {
  "title": "Fieldset",
  "description": "Semantic fieldset container with a legend prop. Renders a native <fieldset> with a proper <legend> element for accessible group labeling.",
  "category": "form-inputs",
  "files": [{ "src": "components/fieldset.tsx", "dst": "fieldset.tsx" }],
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  "registryDeps": []
},
"form": {
  "title": "Form",
  "description": "Thin form wrapper with onSubmit passthrough. Composes Field + Input inside as children. Built on Base UI Form.",
  "category": "form-inputs",
  "files": [{ "src": "components/form.tsx", "dst": "form.tsx" }],
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  "registryDeps": []
},
"input": {
  "title": "Input",
  "description": "Text input with inline label, helper text, error text, icon (left or right), and size variants (sm/md/lg). Ref forwards to the native <input> for react-hook-form compatibility.",
  "category": "form-inputs",
  "files": [{ "src": "components/input.tsx", "dst": "input.tsx" }],
  "deps": ["@base-ui/react", "clsx", "lucide-react", "tailwind-merge"],
  "registryDeps": []
},
"switch": {
  "title": "Switch",
  "description": "Toggle switch with animated thumb. Accepts checked/defaultChecked and onCheckedChange. Renders a label alongside the track.",
  "category": "form-inputs",
  "files": [{ "src": "components/switch.tsx", "dst": "switch.tsx" }],
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  "registryDeps": []
},
"toggle": {
  "title": "Toggle",
  "description": "Pressable toggle button with three variants (default, outline, ghost) and three sizes (sm/md/lg). Uses data-pressed for pressed-state styling.",
  "category": "form-inputs",
  "files": [{ "src": "components/toggle.tsx", "dst": "toggle.tsx" }],
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  "registryDeps": []
},
"text-area": {
  "title": "TextArea",
  "description": "Multiline text input with inline label, helper text, error text, optional char count display, and rows/maxLength props. Ref forwards to the native <textarea>.",
  "category": "form-inputs",
  "files": [{ "src": "components/text-area.tsx", "dst": "text-area.tsx" }],
  "deps": ["@base-ui/react", "clsx", "tailwind-merge"],
  "registryDeps": []
}
```

Write the complete registry.json with all 12 entries (5 existing + 7 new). Do NOT drop or modify any of the 5 existing Phase 1 entries. The JSON must be valid (no trailing commas, proper array/object syntax).

Confirm `lucide-react` appears ONLY in the `input` entry — not in field, fieldset, form, switch, toggle, or text-area.
  </action>
  <verify>
    <automated>node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(Object.keys(r).length, Object.keys(r).join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - registry.json is valid JSON (node -e parses without error)
    - registry.json contains exactly 12 top-level keys
    - registry.json contains `"field"` key with `"deps": ["@base-ui/react", "clsx", "tailwind-merge"]`
    - registry.json contains `"fieldset"` key with `"deps": ["@base-ui/react", "clsx", "tailwind-merge"]`
    - registry.json contains `"form"` key with `"deps": ["@base-ui/react", "clsx", "tailwind-merge"]`
    - registry.json contains `"input"` key with `"deps": ["@base-ui/react", "clsx", "lucide-react", "tailwind-merge"]`
    - registry.json contains `"switch"` key with `"deps": ["@base-ui/react", "clsx", "tailwind-merge"]`
    - registry.json contains `"toggle"` key with `"deps": ["@base-ui/react", "clsx", "tailwind-merge"]`
    - registry.json contains `"text-area"` key with `"deps": ["@base-ui/react", "clsx", "tailwind-merge"]`
    - All 7 new entries have `"registryDeps": []`
    - All 7 new entries have `"category": "form-inputs"`
    - All 5 Phase 1 entries (button, separator, avatar, progress, meter) are unchanged
    - Only the `input` entry contains `"lucide-react"` among the 7 new entries
    - `grep -c '"registryDeps": \[\]' packages/ui/src/registry/versions/0.5.0/registry.json` returns `12` (all 12 entries have empty registryDeps)
  </acceptance_criteria>
  <done>registry.json has 12 valid entries covering all Phase 1 and Phase 2 components. All deps and registryDeps are correct per the research-verified specification. `bct add field/fieldset/form/input/switch/toggle/text-area` will resolve to the correct component files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry.json → bct CLI | The CLI reads registry.json to resolve component file paths and deps; malformed JSON or incorrect deps would cause installation failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | registry.json deps list | mitigate | Validate JSON structure in acceptance criteria; deps are a closed set (4 allowed packages from CLAUDE.md constraints); acceptance criteria checks that only input has lucide-react |
| T-02-14 | Information Disclosure | registry.json | accept | registry.json is a public manifest; component names, file paths, and dep lists are intended to be public (consumers read this to install components) |
</threat_model>

<verification>
After task completes:
- `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(Object.keys(r).length)"` outputs `12`
- `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.input.deps.includes('lucide-react'))"` outputs `true`
- `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.field.deps.includes('lucide-react'))"` outputs `false`
- `node -e "const r = require('./packages/ui/src/registry/versions/0.5.0/registry.json'); console.log(r.button.title)"` outputs `Button` (Phase 1 entry unchanged)
- `pnpm --filter @bctechnology/ui test` still passes (all 12 tests, no regression from registry.json edit)
</verification>

<success_criteria>
`registry.json` contains 12 valid entries covering all Phase 1 and Phase 2 components. Correct deps per component (lucide-react only for input). All registryDeps are empty arrays. Phase 1 entries unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-basics/02-05-SUMMARY.md`
</output>

---
phase: 00-preparation-api-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/assets/tokens/index.css
autonomous: true
requirements:
  - PREP-02
  - PREP-03

must_haves:
  truths:
    - "index.css contains the two missing top/bottom slide keyframes needed by Drawer and Toast"
    - "index.css has --z-toast: 120 in the @theme block so Toast can layer above open dialogs"
    - "index.css has --color-border-muted and --color-border-muted-hover values in both :root and .dark blocks"
    - "All 13 animation class families (.bct-drawer-*, .bct-tooltip-popup, .bct-popover-popup, .bct-menu-popup, .bct-select-popup, .bct-combobox-popup, .bct-toast-item, .bct-alert-dialog-*) are present in index.css with correct data-open/data-closed rules"
    - "Every [data-closed] animation rule uses the forwards fill-mode keyword"
  artifacts:
    - path: "packages/ui/src/assets/tokens/index.css"
      provides: "Overlay animation class families for all Phase 5–7 components + token gap fixes"
      contains: "bct-drawer-right"
  key_links:
    - from: "packages/ui/src/assets/tokens/index.css"
      to: "packages/ui/src/registry/versions/0.5.0/components/"
      via: "Components apply animation class as first arg to twMerge on the animated element"
      pattern: "bct-drawer|bct-tooltip|bct-popover|bct-menu|bct-select|bct-combobox|bct-toast|bct-alert-dialog"
---

<objective>
Extend `packages/ui/src/assets/tokens/index.css` with: (1) four missing slide keyframes for top/bottom Drawer and Toast, (2) the `--z-toast` z-index token, (3) missing `--color-border-muted` / `--color-border-muted-hover` values in light and dark theme blocks, and (4) all 13 new animation class families for Phase 5–7 overlay components.

Purpose: These CSS additions are the only code changes in Phase 0. All subsequent phase component implementations depend on these animation classes being present — Phase 5 (Overlays) must not need to edit `index.css` to set up animations.

Output: Updated `packages/ui/src/assets/tokens/index.css` with new keyframes in `@theme`, token gap fixes in `@layer base`, and 13 animation class families after the existing `.bct-dialog-*` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-preparation-api-design/00-CONTEXT.md
@.planning/phases/00-preparation-api-design/00-RESEARCH.md
@.planning/phases/00-preparation-api-design/00-UI-SPEC.md
</context>

<interfaces>
<!-- Current state of index.css that executor MUST understand before editing -->

Existing z-index block in @theme (lines 234–240 of index.css):
```css
/* //? Z-index Tokens */
--z-header: 50;
--z-dropdown: 50;
--z-popover: 50;
--z-tooltip: 60;
--z-dialog-backdrop: 100;
--z-dialog-popup: 110;
```
ADD after --z-dialog-popup: `--z-toast: 120;`

Existing animation tokens block in @theme (lines 214–232 of index.css, partial):
```css
--animate-slide-in-from-right: slide-in-from-right 300ms cubic-bezier(0, 0, 0.2, 1);
--animate-slide-out-to-right: slide-out-to-right 300ms cubic-bezier(0.4, 0, 1, 1);
--animate-slide-in-from-left: slide-in-from-left 300ms cubic-bezier(0, 0, 0.2, 1);
--animate-slide-out-to-left: slide-out-to-left 300ms cubic-bezier(0.4, 0, 1, 1);
--animate-scale-in: scale-in 200ms cubic-bezier(0, 0, 0.2, 1);
--animate-scale-out: scale-out 150ms cubic-bezier(0.4, 0, 1, 1);
```
ADD four new animate tokens for the new keyframes (after --animate-scale-out):
```css
--animate-slide-in-from-top: slide-in-from-top 300ms cubic-bezier(0, 0, 0.2, 1);
--animate-slide-out-to-top: slide-out-to-top 300ms cubic-bezier(0.4, 0, 1, 1);
--animate-slide-in-from-bottom: slide-in-from-bottom 300ms cubic-bezier(0, 0, 0.2, 1);
--animate-slide-out-to-bottom: slide-out-to-bottom 300ms cubic-bezier(0.4, 0, 1, 1);
```

Existing keyframes — the last keyframe in the @theme block is scale-out (ends around line 411):
```css
@keyframes scale-out {
    0% { transform: scale(1); opacity: 1; }
    100% { transform: scale(0.95); opacity: 0; }
}
```
ADD four new @keyframes after scale-out, before the closing `}` of @theme:
```css
@keyframes slide-in-from-top {
    0% { transform: translateY(-100%); opacity: 0; }
    100% { transform: translateY(0); opacity: 1; }
}
@keyframes slide-out-to-top {
    0% { transform: translateY(0); opacity: 1; }
    100% { transform: translateY(-100%); opacity: 0; }
}
@keyframes slide-in-from-bottom {
    0% { transform: translateY(100%); opacity: 0; }
    100% { transform: translateY(0); opacity: 1; }
}
@keyframes slide-out-to-bottom {
    0% { transform: translateY(0); opacity: 1; }
    100% { transform: translateY(100%); opacity: 0; }
}
```

Existing :root light block — border tokens (no --color-border-muted values present):
```css
--color-border: #e5e5e5;
--color-border-hover: #737373;
--color-border-focus: #a3a3a3;
--color-border-disabled: #e5e5e5;
--color-divider: rgba(10, 10, 10, 0.1);
```
ADD after --color-border-disabled (before --color-divider):
```css
--color-border-muted: #f0f0f0;
--color-border-muted-hover: #e0e0e0;
```

Existing .dark block — border tokens (no --color-border-muted values present):
```css
--color-border: #404040;
--color-border-hover: #737373;
--color-border-focus: #a3a3a3;
--color-border-disabled: #333333;
--color-divider: rgba(240, 240, 240, 0.25);
```
ADD after --color-border-disabled (before --color-divider):
```css
--color-border-muted: #525252;
--color-border-muted-hover: #666666;
```

Existing animation classes block — ends at line 1019 (after .bct-dialog-panel-left[data-closed] rule):
```css
/* Side panel — left */
.bct-dialog-panel-left[data-open] {
    animation: slide-in-from-left 300ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-dialog-panel-left[data-closed] {
    animation: slide-out-to-left 300ms cubic-bezier(0.4, 0, 1, 1) forwards;
}
```
ADD all 13 new animation class families AFTER this block (see Task 2 action for exact CSS).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add missing keyframes, animation tokens, z-index token, and border-muted values to index.css</name>
  <files>packages/ui/src/assets/tokens/index.css</files>
  <read_first>
    - packages/ui/src/assets/tokens/index.css (MANDATORY — read the full file to understand exact current structure before editing; identify exact line numbers for insertion points)
    - .planning/phases/00-preparation-api-design/00-UI-SPEC.md (Animation Class Contract section — missing keyframes table)
  </read_first>
  <action>
Make four targeted edits to `packages/ui/src/assets/tokens/index.css`. Read the file first to find exact line numbers. All edits are additive — do NOT remove or modify existing content.

**Edit 1 — Add --z-toast to @theme z-index block:**
Find the line `--z-dialog-popup: 110;` in the `/* //? Z-index Tokens */` block.
Add the following line IMMEDIATELY AFTER `--z-dialog-popup: 110;`:
```css
	--z-toast: 120;
```
Use a tab for indentation, matching the existing z-index lines.

**Edit 2 — Add four new animation tokens to @theme animation block:**
Find the line `--animate-scale-out: scale-out 150ms cubic-bezier(0.4, 0, 1, 1);` in the `/* //? Animation Tokens */` block.
Add the following four lines IMMEDIATELY AFTER `--animate-scale-out`:
```css
	--animate-slide-in-from-top: slide-in-from-top 300ms cubic-bezier(0, 0, 0.2, 1);
	--animate-slide-out-to-top: slide-out-to-top 300ms cubic-bezier(0.4, 0, 1, 1);
	--animate-slide-in-from-bottom: slide-in-from-bottom 300ms cubic-bezier(0, 0, 0.2, 1);
	--animate-slide-out-to-bottom: slide-out-to-bottom 300ms cubic-bezier(0.4, 0, 1, 1);
```

**Edit 3 — Add four new @keyframes inside the @theme block:**
Find the closing `}` of the `@keyframes scale-out` block (the last keyframe defined). Add the following four keyframe definitions AFTER `scale-out`'s closing brace, but BEFORE the closing `}` that ends the entire `@theme { }` block:

```css

	@keyframes slide-in-from-top {
		0% {
			transform: translateY(-100%);
			opacity: 0;
		}
		100% {
			transform: translateY(0);
			opacity: 1;
		}
	}

	@keyframes slide-out-to-top {
		0% {
			transform: translateY(0);
			opacity: 1;
		}
		100% {
			transform: translateY(-100%);
			opacity: 0;
		}
	}

	@keyframes slide-in-from-bottom {
		0% {
			transform: translateY(100%);
			opacity: 0;
		}
		100% {
			transform: translateY(0);
			opacity: 1;
		}
	}

	@keyframes slide-out-to-bottom {
		0% {
			transform: translateY(0);
			opacity: 1;
		}
		100% {
			transform: translateY(100%);
			opacity: 0;
		}
	}
```

Use tabs for indentation matching the existing keyframe blocks (outer level = 1 tab, property lines = 2 tabs).

**Edit 4a — Add border-muted values to :root light block:**
Find the `:root` / `[data-theme="light"]` block. Find the line `--color-border-disabled: #e5e5e5;`.
Add the following two lines IMMEDIATELY AFTER `--color-border-disabled: #e5e5e5;`:
```css
		--color-border-muted: #f0f0f0;
		--color-border-muted-hover: #e0e0e0;
```
Use two tabs for indentation, matching the existing border token lines in this block.

**Edit 4b — Add border-muted values to .dark block:**
Find the `.dark` / `[data-theme="dark"]` block. Find the line `--color-border-disabled: #333333;`.
Add the following two lines IMMEDIATELY AFTER `--color-border-disabled: #333333;`:
```css
		--color-border-muted: #525252;
		--color-border-muted-hover: #666666;
```
Use two tabs for indentation, matching the existing border token lines in this block.
  </action>
  <verify>
    <automated>grep -c "slide-in-from-top\|slide-out-to-top\|slide-in-from-bottom\|slide-out-to-bottom" packages/ui/src/assets/tokens/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "z-toast" packages/ui/src/assets/tokens/index.css` returns exactly 1 match with value 120
    - `grep "slide-in-from-top" packages/ui/src/assets/tokens/index.css` returns at least 2 matches (1 in @theme animate token, 1 in @keyframes)
    - `grep "slide-in-from-bottom" packages/ui/src/assets/tokens/index.css` returns at least 2 matches
    - `grep "slide-out-to-top" packages/ui/src/assets/tokens/index.css` returns at least 2 matches
    - `grep "slide-out-to-bottom" packages/ui/src/assets/tokens/index.css` returns at least 2 matches
    - `grep "color-border-muted:" packages/ui/src/assets/tokens/index.css` returns exactly 2 matches (light + dark theme values)
    - `grep "color-border-muted-hover:" packages/ui/src/assets/tokens/index.css` returns exactly 2 matches (light + dark theme values)
    - `grep "#f0f0f0" packages/ui/src/assets/tokens/index.css` returns at least 1 match (light border-muted value)
    - `grep "#525252" packages/ui/src/assets/tokens/index.css` returns at least 1 match (dark border-muted value)
    - `pnpm biome check packages/ui/src/assets/tokens/index.css` exits with code 0 (no lint/format errors)
  </acceptance_criteria>
  <done>index.css contains --z-toast: 120, all four new slide keyframes, four new animate tokens, and border-muted values in both light and dark theme blocks. Biome check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add 13 animation class families to index.css after .bct-dialog-* block</name>
  <files>packages/ui/src/assets/tokens/index.css</files>
  <read_first>
    - packages/ui/src/assets/tokens/index.css (re-read to find the exact end of the .bct-dialog-panel-left block — insertion point for new families)
    - .planning/phases/00-preparation-api-design/00-UI-SPEC.md (Animation Class Contract section — complete table of 13 families with exact timing values)
    - .planning/phases/00-preparation-api-design/00-RESEARCH.md (Pattern 2 — animation class families table and code examples)
  </read_first>
  <action>
Append 13 animation class family blocks to the END of `packages/ui/src/assets/tokens/index.css`, after the last line of the `.bct-dialog-panel-left[data-closed]` block.

Write the following CSS EXACTLY as shown — use tab indentation for property lines matching the existing `.bct-dialog-*` blocks. Every `[data-closed]` rule MUST end with `forwards` keyword (D-05):

```css

/* ─── BCT Drawer animations ─────────────────────────────────────────────────── */

/* Drawer — right */
.bct-drawer-right[data-open] {
	animation: slide-in-from-right 300ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-drawer-right[data-closed] {
	animation: slide-out-to-right 300ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* Drawer — left */
.bct-drawer-left[data-open] {
	animation: slide-in-from-left 300ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-drawer-left[data-closed] {
	animation: slide-out-to-left 300ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* Drawer — top */
.bct-drawer-top[data-open] {
	animation: slide-in-from-top 300ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-drawer-top[data-closed] {
	animation: slide-out-to-top 300ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* Drawer — bottom */
.bct-drawer-bottom[data-open] {
	animation: slide-in-from-bottom 300ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-drawer-bottom[data-closed] {
	animation: slide-out-to-bottom 300ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* Drawer backdrop */
.bct-drawer-backdrop[data-open] {
	animation: fade-in 150ms ease-out;
}
.bct-drawer-backdrop[data-closed] {
	animation: fade-out 300ms ease-out forwards;
}

/* ─── BCT Tooltip animations ────────────────────────────────────────────────── */

.bct-tooltip-popup[data-open] {
	animation: scale-in 100ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-tooltip-popup[data-closed] {
	animation: scale-out 75ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* ─── BCT Popover animations (Popover, PreviewCard) ─────────────────────────── */

.bct-popover-popup[data-open] {
	animation: scale-in 150ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-popover-popup[data-closed] {
	animation: scale-out 100ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* ─── BCT Menu animations (Menu, ContextMenu, Menubar) ──────────────────────── */

.bct-menu-popup[data-open] {
	animation: scale-in 150ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-menu-popup[data-closed] {
	animation: scale-out 100ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* ─── BCT Select animations ─────────────────────────────────────────────────── */

.bct-select-popup[data-open] {
	animation: scale-in 150ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-select-popup[data-closed] {
	animation: scale-out 100ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* ─── BCT Combobox animations (Combobox, Autocomplete) ──────────────────────── */

.bct-combobox-popup[data-open] {
	animation: scale-in 150ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-combobox-popup[data-closed] {
	animation: scale-out 100ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* ─── BCT Toast animations ──────────────────────────────────────────────────── */

.bct-toast-item[data-open] {
	animation: slide-in-from-right 300ms cubic-bezier(0, 0, 0.2, 1);
}
.bct-toast-item[data-closed] {
	animation: slide-out-to-right 300ms cubic-bezier(0.4, 0, 1, 1) forwards;
}

/* ─── BCT AlertDialog animations ────────────────────────────────────────────── */

/* AlertDialog backdrop */
.bct-alert-dialog-backdrop[data-open] {
	animation: fade-in 150ms ease-out;
}
.bct-alert-dialog-backdrop[data-closed] {
	animation: fade-out 300ms ease-out forwards;
}

/* AlertDialog popup */
.bct-alert-dialog-popup[data-open] {
	animation: slide-up 150ms ease-out;
}
.bct-alert-dialog-popup[data-closed] {
	animation: scale-out 150ms cubic-bezier(0.4, 0, 1, 1) forwards;
}
```

The 13 class families are: bct-drawer-right, bct-drawer-left, bct-drawer-top, bct-drawer-bottom, bct-drawer-backdrop, bct-tooltip-popup, bct-popover-popup, bct-menu-popup, bct-select-popup, bct-combobox-popup, bct-toast-item, bct-alert-dialog-backdrop, bct-alert-dialog-popup.
  </action>
  <verify>
    <automated>grep -c "bct-drawer\|bct-tooltip\|bct-popover\|bct-menu\|bct-select\|bct-combobox\|bct-toast\|bct-alert-dialog" packages/ui/src/assets/tokens/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bct-drawer-right" packages/ui/src/assets/tokens/index.css` returns 2 (one [data-open], one [data-closed])
    - `grep -c "bct-drawer-left" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-drawer-top" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-drawer-bottom" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-drawer-backdrop" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-tooltip-popup" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-popover-popup" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-menu-popup" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-select-popup" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-combobox-popup" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-toast-item" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-alert-dialog-backdrop" packages/ui/src/assets/tokens/index.css` returns 2
    - `grep -c "bct-alert-dialog-popup" packages/ui/src/assets/tokens/index.css` returns 2
    - Every [data-closed] rule contains `forwards`: `grep "\[data-closed\]" -A1 packages/ui/src/assets/tokens/index.css | grep -c "forwards"` returns ≥ 13
    - `pnpm biome check packages/ui/src/assets/tokens/index.css` exits with code 0
  </acceptance_criteria>
  <done>index.css contains all 13 animation class families. Every [data-closed] rule uses forwards fill-mode. Biome check passes. Phase 5–7 component implementations can apply animation classes without needing to edit index.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system write | Executor edits a CSS design-token file — no user input, no external data sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Tampering | index.css animation classes | accept | CSS file only; no executable code; not a security boundary; low-value target |
| T-00-04 | Denial of Service | Malformed CSS breaking the build | mitigate | Run `pnpm biome check packages/ui/src/assets/tokens/index.css` after each task; acceptance criteria requires exit 0 |

Security domain not applicable to this plan. Phase 0 Plan 02 edits only a CSS design-token file. No authentication, user input, cryptography, session management, or deployed code is involved.
</threat_model>

<verification>
After both tasks complete:

1. New keyframes present: `grep -c "slide-in-from-top\|slide-out-to-top\|slide-in-from-bottom\|slide-out-to-bottom" packages/ui/src/assets/tokens/index.css` returns ≥ 8 (4 in animate tokens, 4 in @keyframes)
2. z-toast token: `grep "z-toast" packages/ui/src/assets/tokens/index.css` returns 1 match
3. border-muted fixed: `grep -c "color-border-muted:" packages/ui/src/assets/tokens/index.css` returns 2
4. All 13 families: `grep -c "\[data-open\]" packages/ui/src/assets/tokens/index.css` returns ≥ 17 (4 existing + 13 new)
5. forwards fill-mode: `grep "\[data-closed\]" -A1 packages/ui/src/assets/tokens/index.css | grep -c "forwards"` returns ≥ 13
6. Biome clean: `pnpm biome check packages/ui/src/assets/tokens/index.css` exits 0
</verification>

<success_criteria>
- index.css contains all four new keyframes (slide-in-from-top, slide-out-to-top, slide-in-from-bottom, slide-out-to-bottom)
- index.css contains --z-toast: 120 in the @theme block
- index.css has --color-border-muted and --color-border-muted-hover values in both :root and .dark blocks
- All 13 animation class families are present with correct [data-open] and [data-closed] rules
- Every single [data-closed] rule ends with `forwards`
- Biome check passes — no lint or format errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/00-preparation-api-design/00-02-SUMMARY.md` following the summary template.
</output>
